#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main(){
    double K1, K2, S, T, res;
    scanf("%lf %lf %lf %lf", &K1, &K2, &S, &T);
    res = abs(S-(K1*T+K2*T));
    printf("%lf", res);
}
